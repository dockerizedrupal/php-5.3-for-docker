export PATH="${HOME}/.composer/vendor/bin:${PATH}"

<% if @drush_version == "6" %>
. /etc/bash_completion.d/drush.complete.sh
<% end %>

<% if @drush_version == "7" %>
. /etc/bash_completion.d/drush.complete.sh
<% end %>
